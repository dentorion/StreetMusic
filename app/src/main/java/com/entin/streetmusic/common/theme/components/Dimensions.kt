package com.entin.streetmusic.common.theme.components

import androidx.compose.ui.unit.dp
import com.entin.streetmusic.common.theme.StreetMusicDimensions

val dimensionsPalette = StreetMusicDimensions(

    /**
     * Start, Permission, CityConcerts, Artist screen
     */

    // Horizontal padding for all content
    allHorizontalPadding = 15.dp,

    // Vertical padding for all content
    allVerticalPadding = 10.dp,

    // Bottom lift up buttons
    allBottomPadding = 45.dp,

    // Buttons vertical padding
    buttonsVerticalPadding = 5.dp,

    // Divider thickness
    dividerThickness = 2.dp,

    // Concert row vertical padding in list
    concertRowVerticalPadding = 30.dp,

    // Image size: style music and map in row
    concertRowImageSize = 30.dp,

    // Space between Band name and Create date
    concertRowSpaceBetweenInColumn = (-5).dp,

    // Border stroke active button
    borderStrokeActiveButton = 1.dp,

    // Padding between buttons
    paddingBetweenButtons = 5.dp,

    // Status Green / Red circle size
    sizeStatusCircle = 9.dp,

    // Now / Last playing vertical padding
    statusPlayingVerticalPadding = 25.dp,

    // Now / Last playing horizontal padding
    statusPlayingHorizontalPadding = 10.dp,

    // Big avatar artist size
    avatarSize = 128.dp,

    // Like circle image on avatar size
    likeSizeCircle = 42.dp,

    // Like heart image on avatar size
    likeSizeHeart = 25.dp,

    // Like heart image top padding on avatar size
    likeHeartTopPadding = 2.dp,

    // Vertical line green / red
    verticalDividerThickness = 1.dp,

    // Top padding row concert description
    rowTopPaddingConcertDescription = 15.dp,

    // Inner padding for white circle inside to show music style in Artist
    innerPaddingMusicStyle = 5.dp,

    // Start padding in concert details in Artist
    startPaddingConcertDetails = 25.dp,

    // Vertical padding for concert details in Artist
    verticalPaddingConcertDetails = 15.dp,

    // Elevation crd concert details in Artist
    elevationCard = 0.dp,

    // Border thickness for auth button in Authorization
    authBorderThickness = 1.dp,

    // Horizontal padding for content in PreConcert
    horizontalPaddingPre = 35.dp,

    // Star size in PreConcert
    starSizePre = 20.dp,

    // Label bottom padding in PreConcert
    labelPadding = 2.dp,

    // Music style size button in PreConcert
    musicStyleButtonSize = 45.dp,

    // Music style size icon in PreConcert
    musicStyleImageSize = 22.dp,

    // Divider top padding in Concert
    dividerTopPaddingConcert = 10.dp,

    // Divider thickness in Dialog
    dividerThicknessDialog = 1.dp,

    // Arrow back in MapObserve
    backIconSize = 14.dp
)